---
label: 레시피앱-6-레시피앱 제작준비
expanded: true
order: 100
author:
name: 코알라코딩
avatar: koala
link: https://www.youtube.com/channel/UCMb94yucTNsjIJqD8C8lO2Q
category: [react, project, recipe]
icon: /mdi_react.png
tags: [리액트, react, React, nodejs, NODE, useState, useEffect]
---

### 목차 <!-- omit in toc -->

- [1. 시작파일 내려받기](#1-시작파일-내려받기)
- [2. App 컴포넌트 비우기](#2-app-컴포넌트-비우기)
- [3. 데이터 로딩 상태 표시해주기](#3-데이터-로딩-상태-표시해주기)
- [4. 조건부렌더링](#4-조건부렌더링)
- [5. 로딩 구현하기](#5-로딩-구현하기)
- [6. 데이터를 어디에 저장할까?](#6-데이터를-어디에-저장할까)
- [7. 여기까지 깃허브에 올리기](#7-여기까지-깃허브에-올리기)
- [8. 완료파일 내려받기](#8-완료파일-내려받기)

---

## 1. 시작파일 내려받기

> 만약 이전 파일이 없을 경우 아래의 파일을 다운로드 하여 진행한다

[!button variant='success' icon='download' text='시작 파일' target='blank'](./assets/06/start.zip)

==- 파일활용방법

1. 다운로드 받은 start.zip 파일의 압축을 푼다
2. 새 리액트 앱을 설치한다. 리액트 앱 설치를 모를경우 아래 링크를 참고한다.
   [!ref target='blank' text='리액트 설치안내' icon='link'](../리액트좀해라/1.md)
3. 리액트 앱의 설치가 왼료 되었으면 1번의 파일압축을 푼다
4. 압축을 풀게 되면 start/src 폴더가 있다.
5. 2번의 src폴더에 4번의 src 폴더를 덮어 씌운다.
   ![](./assets/05-02_643.jpg)
6. 2번의 폴더를 루트로 선택하여 vscode를 연다.
7. vscode 의 터미널에 npm start 를 입력하여 앱을 실행한다.

===

## 2. App 컴포넌트 비우기

1. ./src/App.js 컴포넌트를 깨끗하게 정리하자.

   ```js # App.js
   function App() {
   	return <div className='App'></div>;
   }

   export default App;
   ```

1. index.js 수정

   ```js # index.js
   import React from 'react';
   import ReactDOM from 'react-dom/client';
   import './index.css';
   import App from './App';

   const root = ReactDOM.createRoot(document.getElementById('root'));
   root.render(
   	<>
   		<App />
   	</>
   );
   ```

## 3. 데이터 로딩 상태 표시해주기

1. loading 여부에 따라 App 컴포넌트에 그려야할 내용이 변경되므로 useState Hook을 활용한다

   ```js # App.js
   import { useState } from 'react';
   function App() {
   	const [loading, setLoading] = useState(true);
   	return <div className='App'></div>;
   }

   export default App;
   ```

## 4. 조건부렌더링

1. loading 변수의 값이 true 일경우 app 컴포넌트에 로딩중입니다를 렌더링한다

   ```js #4 App.js
   import { useState } from 'react';
   function App() {
   	const [loading, setLoading] = useState(true);
   	return <div className='App'>{loading && <h1>로딩중입니다...</h1>}</div>;
   }

   export default App;
   ```

   loading 의 값이 true 이므로 로딩중입니다 렌더링된다

## 5. 로딩 구현하기

1. useEffect 훅을 사용하여 0.5초 후 loading 변수의 값을 변경하여 컴포넌트의 내용을 리렌더시킨다
   이제 0.5초후 앱의 타이틀이 출력된다.

   ```js # App.js
   import { useState, useEffect } from 'react';
   function App() {
   	const [loading, setLoading] = useState(true);
   	useEffect(() => {
   		setTimeout(() => {
   			setLoading(false);
   		}, 500);
   	}, []);
   	return <div className='App'>{loading ? <h1>로딩중입니다...</h1> : <h1> 먹기위해하는요리앱</h1>}</div>;
   }

   export default App;
   ```

## 6. 데이터를 어디에 저장할까?

1.  useEffct() 훅의 주석을 같이 작성해보자. 바로 이자리에 레시피 데이터를 로딩 할 것이다..

    ```js #5 App.js
    import { useState, useEffect } from 'react';
    function App() {
    	const [loading, setLoading] = useState(true);
    	useEffect(() => {
    		//레시피데이터로딩
    		setTimeout(() => {
    			setLoading(false);
    		}, 500);
    	}, []);
    	return <div className='App'>{loading ? <h1>로딩중입니다...</h1> : <h1> 요리에몽</h1>}</div>;
    }

    export default App;
    ```

1.  로딩된 데이터는 state에 저장한다
    레시피데이터를 로딩한 다음 state에 저장하려면 어떻게 해야 할까?
    데이터를 저장할 변수가 필요하다. 로딩된 레시피 데이터를 저장할 data 변수를 만들자.
    자료형은 배열이고, 배열 내에 객체로 레시피 데이터가 쭈루룩 들어올 것이다.
    ```js # App.js
    import { useState, useEffect } from 'react';
    function App() {
    const [loading, setLoading] = useState({ state: true, data: [] });
    console.log(loading);
    useEffect(() => {
    //레시피데이터로딩
    setTimeout(() => {
    setLoading(false);
    }, 500);
    }, []);
    return <div className='App'>{loading ? <h1>로딩중입니다...</h1> : <h1> 요리에몽</h1>}</div>;
    }

        export default App;

        ```

## 7. 여기까지 깃허브에 올리기

```bash
	git add .
	git commit -m "06 레시피앱 준비"
	git push origin main
```
## 8. 완료파일 내려받기

[!button variant='success' icon='download' text='완료 파일' target='blank'](./assets/06/06_fin.zip)
